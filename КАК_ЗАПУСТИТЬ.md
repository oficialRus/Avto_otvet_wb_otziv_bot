# 🚀 Как запустить программу

## 📍 Запуск на Windows (локально)

### Способ 1: Через BAT файл (САМЫЙ ПРОСТОЙ)

**Просто дважды кликните на файл:**
```
feedback-bottttt.bat
```

Или в командной строке:
```cmd
feedback-bottttt.bat
```

### Способ 2: Через готовый бинарник

```cmd
set TELEGRAM_TOKEN=8447547104:AAF6IZSfMsYhz33yI9AtWNXogrdKmNDzYtk
set DB_TYPE=sqlite
set DB_PATH=data/feedbacks.db
set REQUIRED_CHANNEL_ID=-1003294078901
set REQUIRED_CHANNEL=novikovpromarket
set ADMIN_USER_ID=7217012505
set LOG_LEVEL=info

feedback-bot.exe
```

### Способ 3: Через go run

```cmd
set TELEGRAM_TOKEN=8447547104:AAF6IZSfMsYhz33yI9AtWNXogrdKmNDzYtk
set DB_TYPE=sqlite
set DB_PATH=data/feedbacks.db
set REQUIRED_CHANNEL_ID=-1003294078901
set REQUIRED_CHANNEL=novikovpromarket
set ADMIN_USER_ID=7217012505
set LOG_LEVEL=info

go run ./cmd/feedback-bot
```

---

## 🐧 Запуск на Linux сервере

### Способ 1: Через скрипт (РЕКОМЕНДУЕТСЯ)

```bash
# Сделать файл исполняемым (первый раз)
chmod +x feedback-bottttt

# Запустить
./feedback-bottttt
```

### Способ 2: Через готовый бинарник

```bash
export TELEGRAM_TOKEN="8447547104:AAF6IZSfMsYhz33yI9AtWNXogrdKmNDzYtk"
export DB_TYPE="sqlite"
export DB_PATH="data/feedbacks.db"
export REQUIRED_CHANNEL_ID="-1003294078901"
export REQUIRED_CHANNEL="novikovpromarket"
export ADMIN_USER_ID="7217012505"
export LOG_LEVEL="info"

./feedback-bot
```

### Способ 3: С PostgreSQL (для production)

```bash
export TELEGRAM_TOKEN="ваш_токен"
export DB_TYPE="postgres"
export DB_PATH="host=localhost port=5432 user=feedback_bot password=*** dbname=feedbacks sslmode=disable"
export REQUIRED_CHANNEL_ID="-1003294078901"
export REQUIRED_CHANNEL="novikovpromarket"
export ADMIN_USER_ID="7217012505"
export LOG_LEVEL="info"
export METRICS_ADDR=":8080"

./feedback-bottttt
```

### Способ 4: Как systemd service (для автозапуска)

Создайте файл `/etc/systemd/system/feedback-bot.service`:

```ini
[Unit]
Description=Feedback Bot для Wildberries
After=network.target postgresql.service

[Service]
Type=simple
User=feedback-bot
WorkingDirectory=/opt/feedback-bot
ExecStart=/opt/feedback-bot/feedback-bottttt
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable feedback-bot
sudo systemctl start feedback-bot
sudo systemctl status feedback-bot
```

---

## ⚙️ Переменные окружения

### Обязательные:
- `TELEGRAM_TOKEN` - токен бота от BotFather

### Опциональные (есть значения по умолчанию):
- `LOG_LEVEL` - уровень логирования (по умолчанию: `info`)
- `DB_TYPE` - тип базы данных (по умолчанию: `sqlite`)
- `DB_PATH` - путь к БД или DSN для PostgreSQL (по умолчанию: `data/feedbacks.db`)
- `REQUIRED_CHANNEL_ID` - ID канала для проверки подписки (по умолчанию: `-1003294078901`)
- `REQUIRED_CHANNEL` - username канала (по умолчанию: `novikovpromarket`)
- `ADMIN_USER_ID` - ID администратора (по умолчанию: `7217012505`)
- `METRICS_ADDR` - адрес для метрик Prometheus (по умолчанию: `:8080`)

---

## 🔍 Проверка работы

После запуска:

1. **Проверьте логи** - должны быть сообщения о запуске бота
2. **Откройте Telegram** - найдите вашего бота
3. **Отправьте `/start`** - бот должен ответить приветственным сообщением
4. **Проверьте метрики** (если включены):
   ```bash
   curl http://localhost:8080/metrics
   ```

---

## 🛑 Остановка программы

- **Windows:** Нажмите `Ctrl+C` в терминале
- **Linux:** Нажмите `Ctrl+C` или через systemd:
  ```bash
  sudo systemctl stop feedback-bot
  ```

---

## ❓ Часто задаваемые вопросы

### Не запускается на Windows?
- Убедитесь, что Go установлен (если используете `go run`)
- Проверьте, что файл `feedback-bot.exe` существует
- Проверьте переменные окружения

### Не запускается на Linux?
- Проверьте права: `chmod +x feedback-bottttt`
- Убедитесь, что Go установлен (если используете `go run`)
- Проверьте переменные окружения: `echo $TELEGRAM_TOKEN`

### Как изменить настройки?
- Отредактируйте файл `feedback-bottttt.bat` (Windows) или `feedback-bottttt` (Linux)
- Или установите переменные окружения перед запуском

### Как запустить в фоне на сервере?
- Используйте `screen` или `tmux`
- Или настройте systemd service (см. выше)

---

## 📚 Дополнительная информация

- `SERVER_REQUIREMENTS.md` - требования для установки на сервер
- `DEPLOYMENT_GUIDE.md` - подробная инструкция по развертыванию
- `README.md` - общая документация проекта

