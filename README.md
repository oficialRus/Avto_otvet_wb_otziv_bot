# Автобот для ответов на отзывы Wildberries

Автоматизированный Telegram-бот для обработки и ответов на отзывы покупателей в маркетплейсе Wildberries. Бот работает полностью внутри Telegram, позволяя настроить все параметры через интерактивный интерфейс. После настройки бот периодически проверяет непрочитанные отзывы, автоматически формирует ответы на основе рейтинга товара и отправляет их через API Wildberries.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Конфигурация](#конфигурация)
- [Telegram бот](#telegram-бот)
- [Метрики и мониторинг](#метрики-и-мониторинг)
- [Структура проекта](#структура-проекта)

## 📖 Описание проекта

Проект представляет собой Telegram-бота на языке Go, который:

- 🤖 **Работает полностью через Telegram** - вся настройка происходит интерактивно через бота
- 📝 **Интерактивная настройка** - бот запрашивает у пользователя:
  1. Токен доступа к API Wildberries
  2. Текст ответа для положительных отзывов (4-5 звезд)
  3. Текст ответа для отрицательных отзывов (1-3 звезды)
- 🔄 Периодически опрашивает API Wildberries на предмет новых непрочитанных отзывов (каждые 10 минут)
- 💾 Сохраняет идентификаторы обработанных отзывов в SQLite базу данных для избежания дублирования
- ⚡ Обеспечивает rate limiting запросов к API (3 запроса в секунду, burst 6)
- 📊 Предоставляет метрики Prometheus для мониторинга

## 🏗 Архитектура

Проект организован по модульному принципу:

```
feedback_bot/
├── cmd/feedback-bot/      # Точка входа приложения
├── internal/
│   ├── config/           # Конфигурация через переменные окружения
│   ├── scheduler/        # Планировщик периодических задач
│   ├── service/          # Бизнес-логика обработки отзывов
│   ├── storage/          # Абстракция хранилища (SQLite реализация)
│   ├── telegram/         # Telegram бот интеграция
│   └── wbapi/            # Клиент для API Wildberries
├── pkg/
│   ├── logger/           # Структурированное логирование (zap)
│   └── metrics/          # Prometheus метрики
└── data/                 # Директория для SQLite базы данных
```

### Основные компоненты:

1. **WB API Client** (`internal/wbapi/`) - HTTP клиент с rate limiting и обработкой ошибок
2. **Storage** (`internal/storage/`) - SQLite хранилище обработанных отзывов
3. **Service** (`internal/service/`) - Основная бизнес-логика:
   - Получение непрочитанных отзывов
   - Выбор шаблона ответа по рейтингу
   - Отправка ответов через API
   - Сохранение обработанных ID
4. **Scheduler** (`internal/scheduler/`) - Периодическое выполнение цикла обработки
5. **Telegram Bot** (`internal/telegram/`) - Главный интерфейс пользователя:
   - Интерактивная настройка через FSM (конечный автомат состояний)
   - Хранение конфигурации пользователей
   - Управление сервисом обработки отзывов
   - Планировщик автоматической обработки

## ⚙️ Требования

- **Go 1.23+** (или совместимая версия)
- **Telegram Bot Token** - обязательно (получите у [@BotFather](https://t.me/BotFather))
- **Токен доступа к API Wildberries** - будет запрошен ботом при первой настройке (требуется право на работу с отзывами, бит 7)

## 📦 Установка

### 1. Клонирование проекта

```bash
git clone <repository-url>
cd Avto_otvet_wb_otziv_bot
```

### 2. Установка зависимостей

```bash
go mod download
```

### 3. Сборка проекта

```bash
go build -o feedback-bot ./cmd/feedback-bot
```

Или для быстрого запуска в режиме разработки:
```bash
go run ./cmd/feedback-bot
```

## ⚙️ Настройка

### Обязательные переменные окружения

**`TELEGRAM_TOKEN`** - Токен Telegram бота (обязательно)

Получите токен у [@BotFather](https://t.me/BotFather) в Telegram:
1. Откройте чат с [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Скопируйте токен для использования в переменной окружения

### Опциональные переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `LOG_LEVEL` | `info` | Уровень логирования: `debug`, `info`, `warn`, `error`, `fatal` |
| `DB_PATH` | `data/feedbacks.db` | Путь к файлу SQLite базы данных |
| `METRICS_ADDR` | `:8080` | Адрес для Prometheus метрик |
| `APP_VERSION` | `dev` | Версия приложения |

**Примечание:** Все остальные настройки (токен WB, шаблоны ответов) настраиваются интерактивно через Telegram бота при первом запуске!

## 🚀 Запуск

### Windows (PowerShell)

```powershell
# Установка обязательной переменной - токен Telegram бота
$env:TELEGRAM_TOKEN = "ваш_токен_telegram_бота"

# Опциональные настройки
$env:LOG_LEVEL = "debug"

# Запуск
go run ./cmd/feedback-bot
```

Или если уже собрали бинарник:
```powershell
$env:TELEGRAM_TOKEN = "ваш_токен_telegram_бота"
./feedback-bot.exe
```

### Linux/macOS (Bash)

```bash
# Установка переменных окружения
export TELEGRAM_TOKEN="ваш_токен_telegram_бота"
export LOG_LEVEL="info"

# Запуск
go run ./cmd/feedback-bot
```

Или если собрали бинарник:
```bash
export TELEGRAM_TOKEN="ваш_токен_telegram_бота"
./feedback-bot
```

### Первая настройка через Telegram

После запуска приложения:

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании)
2. Отправьте команду `/start` или просто начните диалог
3. Бот попросит вас предоставить:
   - **Токен Wildberries** - отправьте токен доступа к API Wildberries с правами на отзывы
   - **Текст для положительных отзывов** - отправьте шаблон ответа для отзывов на 4-5 звезд
   - **Текст для отрицательных отзывов** - отправьте шаблон ответа для отзывов на 1-3 звезды
4. После завершения настройки бот начнет автоматически обрабатывать отзывы каждые 10 минут

**Важно:** Все настройки сохраняются в базе данных и не требуют повторной настройки после перезапуска бота!

## 🤖 Работа с Telegram ботом

### Команды бота

- `/start` или `/help` - Показать справку и список команд
- `/status` - Показать статус сервиса и текущие настройки
- `/settings` - Изменить настройки (токен WB, шаблоны ответов)
- `/run` или `/run_now` - Запустить цикл обработки отзывов вручную
- `/cancel` - Отменить текущую настройку (если вы в процессе настройки)

### Процесс настройки

При первом запуске или использовании команды `/settings` бот проведет вас через интерактивную настройку:

1. **Шаг 1:** Отправьте токен доступа к API Wildberries
   - Токен должен иметь права на работу с отзывами (бит 7)
   - Получить токен можно в личном кабинете продавца Wildberries

2. **Шаг 2:** Отправьте текст ответа для положительных отзывов (4-5 ⭐)
   - Пример: "Спасибо за ваш отзыв и доверие к нашему магазину! Нам очень важно, что вы делитесь своим опытом."

3. **Шаг 3:** Отправьте текст ответа для отрицательных отзывов (1-3 ⭐)
   - Пример: "Здравствуйте! Сожалеем, что товар не оправдал ожиданий. Напишите, поможем решить вашу проблему!"

После завершения настройки бот автоматически начнет работать!

## 📊 Метрики и мониторинг

Сервис предоставляет Prometheus метрики на эндпоинте `/metrics` (по умолчанию `:8080`).

Для просмотра метрик:
```bash
curl http://localhost:8080/metrics
```

Метрики включают стандартные метрики Go (goroutines, memory, etc.) через Prometheus клиентскую библиотеку.

## 📁 Структура проекта

```
.
├── cmd/
│   └── feedback-bot/
│       └── main.go              # Точка входа приложения
├── internal/
│   ├── config/
│   │   └── config.go             # Конфигурация через env переменные
│   ├── scheduler/
│   │   └── scheduler.go          # Планировщик периодических задач
│   ├── service/
│   │   ├── cycle.go              # Основной цикл обработки отзывов
│   │   └── templates.go          # Движок шаблонов ответов
│   ├── storage/
│   │   ├── store.go              # Интерфейс хранилища
│   │   └── sqlite.go             # SQLite реализация
│   ├── telegram/
│   │   └── bot.go                # Telegram бот интеграция
│   └── wbapi/
│       ├── client.go             # HTTP клиент для WB API
│       └── models.go             # Модели данных API
├── pkg/
│   ├── logger/
│   │   └── logger.go             # Zap логгер конфигурация
│   └── metrics/
│       └── prom.go                # Prometheus метрики
├── data/                         # SQLite база данных (создается автоматически)
├── go.mod                        # Go модули
├── go.sum                        # Checksums зависимостей
└── README.md                     # Этот файл
```

## 🔧 Детали реализации

### Rate Limiting

API клиент использует rate limiting: **3 запроса в секунду** с burst размером **6**. Это соответствует рекомендациям API Wildberries и предотвращает блокировки.

### Обработка ошибок

- Ошибки получения отзывов логируются, но не прерывают работу сервиса
- Ошибки отправки ответов для отдельных отзывов не останавливают обработку остальных
- Ошибки сохранения в БД логируются с предупреждением

### База данных

Используется SQLite с WAL режимом для обеспечения конкурентного доступа. База данных автоматически создается при первом запуске в указанной директории (`data/feedbacks.db` по умолчанию).

Таблица `processed` содержит:
- `id` (TEXT PRIMARY KEY) - идентификатор отзыва
- `created_at` (TIMESTAMP) - время обработки

### Graceful Shutdown

Приложение корректно обрабатывает сигналы SIGINT/SIGTERM:
- Останавливает планировщик
- Завершает текущий цикл обработки (если выполняется)
- Закрывает соединения с БД
- Останавливает сервер метрик

## 🐛 Решение проблем

### Ошибка: "TELEGRAM_TOKEN is required"

Убедитесь, что установили переменную окружения `TELEGRAM_TOKEN` перед запуском:
```powershell
$env:TELEGRAM_TOKEN = "ваш_токен_бота"
```

### Ошибка: "wb api error: ..."

Если бот выдает ошибку при обработке отзывов, проверьте:
- Корректность токена Wildberries (используйте `/settings` для изменения)
- Наличие прав на работу с отзывами (бит 7)
- Доступность API Wildberries

### Telegram бот не отвечает

- Проверьте корректность токена бота в переменной окружения
- Убедитесь, что бот запущен и работает
- Отправьте команду `/start` боту
- Проверьте логи приложения на наличие ошибок

### Бот не начинает работу после настройки

- Убедитесь, что вы завершили все 3 шага настройки
- Проверьте статус через `/status`
- Попробуйте запустить обработку вручную через `/run`
- Проверьте логи на наличие ошибок инициализации

### База данных не создается

Убедитесь, что директория `data/` существует или у приложения есть права на создание файлов в указанном пути. База данных создается автоматически при первом запуске.

## 📝 Логирование

Логи структурированы и используют библиотеку zap. Уровень детализации настраивается через `LOG_LEVEL`:

- `debug` - максимальная детализация
- `info` - информационные сообщения (по умолчанию)
- `warn` - предупреждения
- `error` - ошибки
- `fatal` - критичные ошибки (завершает работу)

Формат логов зависит от переменной `GO_ENV`:
- Production режим → JSON формат
- Development режим → читаемый формат

## 📄 Лицензия

[Указать лицензию проекта]

## 👤 Автор

[Указать автора проекта]

## 🤝 Вклад в проект

[Указать информацию о том, как можно внести вклад]

---

**Важно:** При использовании в продакшене убедитесь в безопасности хранения токенов и настроек. Не коммитьте файлы с реальными токенами в систему контроля версий!

