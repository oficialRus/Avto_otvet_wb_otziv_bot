3. Задаём переменные окружения (только для этой сессии)
powershell
Копировать
Редактировать
# Обязательный токен с правом «Отзывы и вопросы» (бит 7)
$env:WB_TOKEN = "eyJhbGciOiJIUzI1NiIs..."

# (опционально) интервалы, логи, шаблоны
$env:POLL_INTERVAL = "2m"   # ≥ 1m
$env:LOG_LEVEL     = "debug"

# (опционально) песочница вместо боевого API
# $env:WB_BASE_URL = "https://feedbacks-api-sandbox.wildberries.ru"

# (опционально) собственный путь к БД, если не хотите 'data/...'
# $env:DB_PATH = "$PWD\feedbacks.db"

# (опционально) тексты ответов
$env:TPL_BAD  = "Здравствуйте! Сожалеем, что товар не оправдал ожиданий. Мы уже работаем над улучшениями."
$env:TPL_GOOD = "Спасибо за высокую оценку! Рады, что вам понравилось."